{
  "name": "myntis-zk-circuits",
  "version": "1.0.0",
  "description": "ZK circuits for Myntis reward claim verification",
  "scripts": {
    "compile": "circom reward_claim.circom --r1cs --wasm --sym --c",
    "setup": "snarkjs groth16 setup reward_claim.r1cs pot12_final.ptau reward_claim_0000.zkey",
    "contribute": "snarkjs zkey contribute reward_claim_0000.zkey reward_claim_final.zkey",
    "export-vkey": "snarkjs zkey export verificationkey reward_claim_final.zkey verification_key.json",
    "generate-verifier": "snarkjs zkey export solidityverifier reward_claim_final.zkey ../contracts/RewardClaimVerifier_generated.sol",
    "test": "node test_circuit.js",
    "test:full": "npm run compile && npm run setup && npm run contribute && npm run export-vkey && npm test"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.0"
  },
  "devDependencies": {
    "circomlib": "^2.0.5"
  }
}
